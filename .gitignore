# OS
.DS_Store

# Java / Gradle
backend/.gradle/
backend/build/
backend/bin/
*.class
*.log

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Node / frontend (added in the frontend round)
node_modules/
frontend/dist/

# Env / secrets
.env
.env.*
!.env.example

# Agent working notes — kept locally, not part of the codebase
HANDOFF.md
# Root contract/instructions kept local-only (anchored so reference/.../CLAUDE.md stays tracked)
/CLAUDE.md
/claude.md
